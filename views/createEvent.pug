extends ./layout
block content
    h1 Create event 

    form(class="form-group",action="/event/createEvent", method="POST", enctype="multipart/form-data" )
        label New name
        input(class="form-control",name="newName",type="text")
        label New image 
        input(class="form-control",name="newImage",type="file",accept="image/*")
        button(class="btn btn-primary") Save 